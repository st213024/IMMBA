-- =============================================================================
-- ① 刪除遷移殘表（程式只吃 public.admin_users；legacy 可安心刪）
-- ② 選擇性清空管理者，讓登入頁再次出現「建立第一位超級管理者」
-- =============================================================================
-- 使用：Supabase → SQL Editor → 先執行「區塊一」；若要從零授權再取消註解「區塊二」後執行。
-- 區塊二不會刪除 Auth 帳號；若要連帳號重註冊，請到 Authentication → Users 手動刪除使用者。
-- =============================================================================

-- 區塊一：刪除 admin_users_legacy_pre_uuid（無此表時不報錯）
DROP TABLE IF EXISTS public.admin_users_legacy_pre_uuid;

-- 區塊二（可選）：清空 admin_users 所有列 —— 所有人暫時無法進後台，直到再次 bootstrap / SQL 寫入
-- DELETE FROM public.admin_users;
